# very-nice-mermaid

A framework-agnostic **Mermaid flowchart** renderer: parse the DSL, auto-layout
with dagre, and render **beautiful, interactive, themeable** diagrams — no
`mermaid.js` runtime, no headless browser.

> Work in progress. Full docs land with the finished build.
