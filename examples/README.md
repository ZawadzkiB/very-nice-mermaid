# Examples

> Auto-generated by `npm run examples` — **do not edit by hand.**
> Every diagram type rendered in each `--style` (clean / sketch) and theme (light / dark).
> Regenerated on each run so the gallery always matches the current renderer.

## Class

```mermaid
classDiagram
  Animal <|-- Dog
  Animal <|-- Cat
  Animal : +int age
  Animal : +makeSound()
  class Dog { +bark() }
  class Cat { +meow() }
```

| | Clean | Sketch |
|---|---|---|
| **Light** | ![class clean light](./png/class-clean-light.png) | ![class sketch light](./png/class-sketch-light.png) |
| **Dark** | ![class clean dark](./png/class-clean-dark.png) | ![class sketch dark](./png/class-sketch-dark.png) |

## Flowchart

```mermaid
flowchart TD
  start([Push to main]) --> lint{Lint passes?}
  lint -->|yes| test[Run tests]
  lint -->|no| fail[(Report failure)]
  test --> deploy{{Deploy?}}
  deploy -->|prod| prod[/Ship to prod/]
  deploy -->|staging| stg[Ship to staging]
  prod --> done((Done))
  stg --> done
  fail --> done
```

| | Clean | Sketch |
|---|---|---|
| **Light** | ![flowchart clean light](./png/flowchart-clean-light.png) | ![flowchart sketch light](./png/flowchart-sketch-light.png) |
| **Dark** | ![flowchart clean dark](./png/flowchart-clean-dark.png) | ![flowchart sketch dark](./png/flowchart-sketch-dark.png) |

## Sequence

```mermaid
sequenceDiagram
  participant U as User
  participant A as API
  participant DB as Database
  U->>A: POST /login
  A->>DB: lookup user
  DB-->>A: user record
  A->>A: verify password
  A-->>U: 200 + JWT
```

| | Clean | Sketch |
|---|---|---|
| **Light** | ![sequence clean light](./png/sequence-clean-light.png) | ![sequence sketch light](./png/sequence-sketch-light.png) |
| **Dark** | ![sequence clean dark](./png/sequence-clean-dark.png) | ![sequence sketch dark](./png/sequence-sketch-dark.png) |

## State

```mermaid
stateDiagram-v2
  [*] --> Idle
  Idle --> Loading : fetch
  Loading --> Ready : 2xx
  Loading --> Error : fail
  Error --> Loading : retry
  Ready --> [*]
```

| | Clean | Sketch |
|---|---|---|
| **Light** | ![state clean light](./png/state-clean-light.png) | ![state sketch light](./png/state-sketch-light.png) |
| **Dark** | ![state clean dark](./png/state-clean-dark.png) | ![state sketch dark](./png/state-sketch-dark.png) |

