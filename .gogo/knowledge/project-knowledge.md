# Project knowledge

**Purpose:** what this project is — architecture, domains, and the key decisions
the plan phase must respect.

<!-- gogo:meta
Mode: owned            # no upstream doc exists yet (greenfield repo)
Source: [ ]
Confidence: low
Generated-by: /gogo:build (2026-07-03)
-->
> Synthesized from the codebase — which is currently **empty**. Re-run
> `/gogo:build` once code and a README exist to wire real sources.

## What this project is
**Greenfield / empty repository.** No source code, README, or manifest exists
yet. The only file present is `.claude/settings.local.json`. The repo name
(`very-nice-mermaid`) suggests a Mermaid-diagram-related project, but nothing
in the codebase confirms that — treat it as a hint, not a fact.

## Architecture
None yet — to be defined by the first `/gogo:plan`.

## Domains & glossary
None yet.

## Key decisions
- None recorded. The first feature plan will effectively set the initial
  architecture and stack; capture those decisions here (or in a README that
  this file can then proxy).

## gogo overrides
<!-- gogo-specific notes not in the linked source. Preserved across re-runs. -->
