# State — feature `hybrid-diagram-engine`

<!-- Files in this folder (.gogo/work/feature-<slug>/):
  - plan.md        — the accepted plan (the contract) + the feature's functional requirements
  - adjustments.md — log of changes / clarifications you asked for during planning
  - state.md       — THIS file: current phase / status / iterations; lets work resume
  - decisions.md   — forks that needed your call + gogo's recommendation + your answer
  - review/issues.json — living, typed review findings (the contract; see templates/contracts/)
  - review-NN.md   — each code-review round's rendered snapshot of issues.json
  - test/issues.json   — living, typed test findings (same contract)
  - test-NN.md     — each test round's rendered snapshot
  - report/        — the as-built bundle (written at phase ⑤): report.md + the UML set (.mmd) + report/before/ (the plan-time "before" set, copied in for before/after compare) + diagrams.html + result.json
  - charts/        — mermaid diagrams (.mmd) + charts/before/ (the plan-time as-is baseline) + manifest.json + an offline diagrams.html viewer
  (and at the .gogo/ level: .gogo/changelog/<YYYY-MM-DD>-<slug>/ — the shipped report bundle archived by /gogo:done)
-->

- **feature:** hybrid diagram engine — detectType router + mermaid.js fallback for all types + native re-skinned renderers (sequence/class/state/journey), ASCII for flowchart+sequence, transparent fallback diagnostics
- **phase:** done            <!-- plan | implement | review | test | knowledge | done -->
- **status:** shipped        <!-- awaiting-plan-acceptance | plan-accepted | implementing | reviewing | testing | waiting-for-user | done | shipped | aborted -->
- **created:** 2026-07-04
- **branch:** master (repo now a git repo; v0.1.0 shipped)
- **iterations:** plan=1 · implement=6 · review=2 · test=2
- **implement round 6 FIX PASS (2026-07-04):** both test-round-2 findings fixed, each committed, all suites green — `build` ✅ · `typecheck` ✅ · `test` **266 pass** (was 258, +8 new regression tests) · `test:e2e` **33 pass** (Chromium). Browser-safe core re-verified on fresh dist: `dist/index.js` + `dist/element.js` still carry **no static Node/external import** — only the pre-existing lazy `import('mermaid')`/`import('jsdom')`/`import('@resvg/resvg-js')` (the runtime fix added zero imports; it's self-contained). No v1/foundation/native-security/verified-finding regressions (full suite run).
  - **TEST-003 (major, reopened → FIXED, commit c6bec9a) — port offsets now reach the interactive runtime.** `src/render/dom/runtime.ts` gained `computePorts()`, a twin of `geometry.computePortOffsets` (channel offsets + the TEST-006 label stagger) that recomputes from the **live** positions every render — so `mount()`/`mountAsync()`/the `<very-nice-mermaid>` element/the CLI's `-f html` export now render the SAME channel-separated paths as the static SVG, both on initial mount AND after a drag (live re-route no longer collapses edges back onto one channel). `anchor()` slides along the border by the offset (mirroring `sidePoint`/`clampOffset`); `routeEdgePath()` applies channel offset + label shift. **Parity guard fixed:** `test/dom-runtime-parity.test.ts` previously computed its expectation WITHOUT ports (silently encoding the bug — its fan/converging edges spread in the static SVG but not the runtime, yet passed); it now threads `e.ports` and adds an anti-parallel pair, a 3-edge fan, and a **live-drag re-route** (via `importLayout`) all asserting runtime == `computePortOffsets`. **NEW `test/interactive-ports.test.ts`** EXECUTES the real exported HTML in jsdom (not the static SVG) → anti-parallel state edges get distinct paths (no mirror-duplicate), class fan starts at 3 distinct points — closing the exact gap that hid this. HANDS-ON: CLI `-f html` for order-state (pause/resume on distinct 20u channels; start/stop leave/enter Idle at x=135.25/155.25) + shop-class (has/visits/uses at 219.5/239.5/259.5) — all previously collapsed.
  - **TEST-006 (major, new → FIXED, commit 77fbde2) — anti-parallel labels no longer clip.** `computePortOffsets` now takes each labelled edge's plate size and, for edges sharing the **same node pair** (a fan of edges to different targets does NOT group → class case untouched), packs their plates end-to-end along the pair's dominant axis (extent + 6u pad) and centres them; the shift rides on `RoutedEdge.ports.labelShift` (reproduced by `applyPositions` + state pseudo-state shrink), `routeEdge` adds it to the label pos, and the runtime twin recomputes it live. Only the 3 state SVG snapshots moved (labels staggered; **paths identical**); the class fan snapshot is unchanged. Regression tests: TD+LR bidirectional labelled pairs (layout) + state start/stop (state-svg) assert plate rects don't intersect. Repro verified: `go`/`back`, `start`/`stop`, `pause`/`resume` plates all clear by 6u.
- **test round 2 (2026-07-04):** TEST-001 blocker CONFIRMED FIXED (real bundled browser). TEST-002/004/005 verified. **TEST-003 reopened** (port-offset routing landed in static SVG/geometry but NOT in `src/render/dom/runtime.ts` → interactive + CLI `-f html` export still occlude anti-parallel edges / collapse fans) + **TEST-006 new** (static channel spacing 20u < label plate width → labels clip). Fix round = implement 6; MUST cover static AND runtime, + e2e that drives the exported HTML (the gap that hid the runtime miss). **→ RESOLVED in implement round 6 (above).**
- **test round 2 FAIL (2026-07-04):** focused re-test of round 5's fixes. Baseline unchanged-green: `build` ✅ · `typecheck` ✅ · `test` **258 pass** · `test:e2e` **33 pass**. TEST-001 (blocker) **CONFIRMED FIXED** via the mandated real-bundled-browser check: built a throwaway Vite harness (npm-packed `dist/` tarball as a real dependency, served over http://, not `file://`) and drove raw sequence/class/state/pie DSL through `mountAsync` and the `<very-nice-mermaid>` element with the gogo-playwright MCP — all four render correctly (native shapes for sequence/class/state, mermaid `<svg aria-roledescription="pie">` for the fallback), a hostile `<script>`-in-label class DSL rendered inert, zero console errors throughout. TEST-002 (class diamond), TEST-004 (honest fallback failure), TEST-005 (ascii exit-code parity) all **CONFIRMED FIXED** → `verified`. **TEST-003 REOPENED**: the anti-parallel fix (`computePortOffsets`) only reaches the static SVG/PNG renderer (`src/geometry` + `src/layout`) — `src/render/dom/runtime.ts`, the self-contained router `mount()`/`mountAsync()`/the custom element/the CLI's own `-f html` export all actually use, never reads `edge.ports` at all, so the interactive/exported-HTML view still fully occludes anti-parallel transitions (confirmed via the CLI's own `state.html`: `pause`'s path is an exact mirror-duplicate of `resume`'s) and still collapses a fan's edges to one shared trunk (confirmed via `class.html`: `has`/`visits`/`uses` all still start at the identical point). Drag-and-live-reroute mechanism itself is unaffected — only the offset is missing. **New TEST-006 (major)**: the static-SVG fix that IS complete introduced a residual bug — the 20-unit channel spacing is narrower than typical label plates, so adjacent anti-parallel labels' backgrounds overlap and clip each other's text (`start`→`"st"`, `pause`→`"p"`, a flowchart repro's `go` reduced to an unreadable sliver); the class fan case is unaffected (dagre spreads distinct target nodes wide enough). 2 open/new issues (both point at `src/render/dom/runtime.ts` + a label-placement tweak) → back to implement round 6. Full detail: `test-02.md`.
- **test round 1 FAIL (2026-07-04):** blocker TEST-001 (library API/element don't route raw DSL → v1 flowchart parser → garbage for sequence/class/state; e2e missed it — only drove CLI-precomputed HTML); major TEST-002 (class composition diamond bleeds onto sibling edges), TEST-003 (state anti-parallel transitions fully occlude), TEST-004 (fallback types blank/degenerate under jsdom, diagnostic understates); minor TEST-005 (exit-code inconsistency → D8 decided A). Native aesthetics: flowchart+sequence "looks good"; class+state good except edge routing. Fix round = implement 5.
- **implement round 5 FIX PASS (2026-07-04):** ALL 8 findings fixed (test TEST-001..005 + review REV-005/006/007), each committed, all suites green — `build` ✅ · `typecheck` ✅ · `test` **258 pass** (205 unit +53 CLI; was 227, +31 new regression tests) · `test:e2e` **33 pass** (Chromium). Browser-safe-core re-verified on fresh dist: `dist/index.js` + `dist/element.js` carry **no new static Node/external import** — only the pre-existing dynamic `import('mermaid')`/`import('jsdom')`/`import('@resvg/resvg-js')`; the library routing lazy-imports mermaid as before. v1/foundation/native security (REV-001 theme-injection) + determinism guards untouched and green.
  - **TEST-001 (blocker, commit 8b57651) — library + element now route raw DSL.** API decision (**sync-vs-async**, documented in code + README): the sync `renderSvg/renderHtml/renderAscii/renderMarkdown` stay flowchart-fast but now THROW a clear "use the async API" error for an explicit non-flowchart string (via `ensureSyncRenderable` + a lexical `explicitNonFlowchartType`) instead of misparsing it; new async twins `renderSvgAsync/renderHtmlAsync/renderAsciiAsync/renderMarkdownAsync` (`src/render/route.ts`) run `classify()` and dispatch flowchart(sync)/sequence·class·state(native)/fallback(mermaid) exactly like the CLI; `mount()` returns its handle synchronously and finishes a non-flowchart render async via a `DeferredHandle`, `mountAsync()` resolves the settled handle, the fallback tier reuses the `seqRuntime` pan/zoom shell, and the `<very-nice-mermaid>` element now `await`s `mountAsync`. **New `test/route.test.ts`** drives `mount()`, `mountAsync()` AND the element with RAW sequence/class/state/pie (native shapes — no flowchart `.vnm-node` garbage / no fabricated header node; fallback pie → mermaid SVG). NOTE: the element e2e over `file://` still only covers flowchart — a bare `import('mermaid')` can't resolve over `file://` without a bundler/import-map, so non-flowchart element/library routing is covered by the Node unit tests (mermaid resolvable there), the same DOM path a real bundled browser app takes.
  - **TEST-002 + TEST-003 (major, commit 67b435c) — one shared geometry fix.** `computePortOffsets()` (`src/geometry`) groups edge endpoints by (node, border-side) and spreads any 2+ that share a side onto distinct parallel channels (ordered by the other end's position so they don't cross); `sidePoint()` applies a clamped perpendicular offset; offsets ride on `RoutedEdge.ports` so `applyPositions()` + the state pseudo-state shrink reproduce them. Anti-parallel pairs no longer occlude; a fan's relation start-markers each sit on their own edge (composition diamond off the shared trunk). Applied in the shared `layout()` → **flowchart benefits too**. 3 SVG snapshots updated (all legit improvements); regression tests in geometry/layout/class-svg/state-svg.
  - **TEST-004 (major, commit ad946fc) — honest fallback failure (D9-A).** `renderFallbackSvg` detects a degenerate/blank headless render (zero/negative viewBox, extreme aspect, negative-dim rect, empty content) and, under jsdom, emits a clear FR5 `fallback-render-unavailable` **error** + throws instead of returning broken SVG; the CLI exits non-zero and writes **no** svg/HTML file. A real browser (headless=false) still renders these; pie unaffected. No Chromium path (D9-B stays future).
  - **TEST-005 (minor, commit ad946fc) — exit-code normalized (D8-A).** fallback-tier ascii `capability-unavailable` now exits 0 by default / 1 under `--strict`, matching the native tier.
  - **REV-005 (commit 52e8e5b):** state pseudo-states classified on structural signals only (`circle.state-start` / empty-label `default`), never the name → a real state named `end`/`process_start`/`session_end` stays normal with its label.
  - **REV-006 (commit 50f8774):** class relation ends disambiguated via edge-path endpoint geometry when the underscore split is ambiguous (A/A_B/B_C/C) — no silent mis-pairing.
  - **REV-007 (commit a263c3c):** unlabeled sequence messages strip mermaid's U+200B placeholder → label `""`, no stray plate.
- **review round 2 APPROVE (2026-07-04):** 3 open minors/nit to batch — REV-005 (state named `start`/`end`/`*_start` misclassified as `[*]` pseudo-state — classify on structural signals, not name), REV-006 (underscore-heavy class relation ids mis-split `A_B-->C` as `A-->B_C` — disambiguate via edge geometry like state reader), REV-007 (strip U+200B from unlabeled sequence msgs). All agent-fixable; will batch with test findings into one fix round.
- **escalations resolved (orchestrator, 2026-07-04):** D6 journey→fallback (native round-2 = sequence+class+state); D7 jsdom→optionalDependency (deferred to pre-publish polish, not blocking).
- **orchestrator note (2026-07-04):** sequence renderer (round 3) verified green; proceeding to build class+state (round 4) reusing the proven pattern, THEN one review across all native renderers, then a hands-on test pass (visual/aesthetic across flowchart/sequence/class/state + fallback).
- **foundation review CLOSED (orchestrator, 2026-07-04):** round-1 review's 4 findings fixed (round 2) + REV-001 security fix **hands-on verified** by the orchestrator (ran the theme-injection exploit through the built CLI — 0 breakouts, both unsafe values dropped + FR5-reported). Proceeding to native renderers without a separate re-review round (minors have regression tests). Now: implement round 3 = **sequence** native renderer (full vertical slice: SVG-read → model → themed SVG/DOM/ASCII).
- **resume:** implement round 4 (**native CLASS + STATE renderers**) **COMPLETE** (2026-07-04). All green: `build` ✅ · `typecheck` ✅ · `test` **227 pass** (was 195; +32) · `e2e` **33 pass** (25 prior + **4 class + 4 state**, Chromium). Browser-safe-core re-verified on fresh dist (`dist/index.js` only the 3 dynamic imports mermaid/jsdom/resvg — class/state readers use the SAME lazy `loadMermaid` path, no new static Node/external; `dist/element.js` zero bare imports). v1 flowchart + foundation + sequence tests all still pass (untouched); the `dom-runtime-parity` guard stays green (vnmRuntime is reused, not forked). Ready for review round 4 (one review across all native renderers, per the orchestrator note).
  - **What shipped — CLASS (`src/native/class/`):** `read.ts` (mermaid SVG → `ClassModel`, browser-safe `loadMermaid` + `DOMParser`), `card.ts` (shared compartment line layout so the measured box == the drawn compartments), `layout.ts` (build a flowchart `DiagramModel` → reuse `layout()` → `ClassLayout` wrapping a `PositionedModel`), `svg.ts` (compartmented cards + typed UML markers), `interactive.ts` (`buildClassPayload`/`mountClass` reusing flowchart `buildPayload`+`vnmRuntime`), `index.ts`. `src/model/class.ts` (`ClassModel`/`ClassEntity`/`ClassRelation`/`ClassLayout` + `isClassLayout`). Fixture `fixtures/shop-class.mmd`.
  - **What shipped — STATE (`src/native/state/`):** `read.ts` (mermaid SVG → `StateModel`), `layout.ts` (flowchart `DiagramModel` → `layout()` → shrink `[*]` start/end pseudo-states to small circles + re-route their edges → `StateLayout`), `svg.ts` (rounded state cards, start disc / end ring, labeled arrows), `interactive.ts` (`mountState`), `index.ts`. `src/model/state.ts` (`StateModel`/`StateNode`/`StateTransition`/`StateLayout` + `isStateLayout`). Fixture `fixtures/order-state.mmd`.
  - **Shared + wiring:** `src/native/read-util.ts` (DOM read helpers reused by both readers: `hasClass`/`pathPoints`/`parseTranslate` + **`readEdgeLabelMap`** — the geometry-free edge-label→edge `data-id` link). `router.ts` (class→`renderer:"class"`, state→`renderer:"state"`, both tier `native`, NO fallback diagnostic; `NATIVE_PLANNED` now empty). Top-level `renderSvg` dispatches `renderClassSvg`/`renderStateSvg`; `renderHtml` + `mount` reuse the flowchart vnmRuntime path for both (delegate `layout.model`). `cli/run.ts` (`doClassRender`/`doStateRender`; `-f md` → `capability-unavailable` ascii diagnostic, **graceful exit 0**, non-zero only under `--strict`). `src/index.ts` exports the full class/state surface.
  - **CLASS relation-type read fidelity (HIGH — the spike's hard part):** all six UML types recover **cleanly** from the marker + line-pattern combo — inheritance (`<|--`), realization (`..|>`), composition (`*--`), aggregation (`o--`), association (`-->`), dependency (`..>`) — with the correct `head` end (which node gets the triangle/diamond/arrow) and 0 diagnostics on the fixture. `(from,to)` from the relation `data-id` name-split against the known class set (verified robust to `_` in class names, e.g. `Order_Item`). Labels via the bulletproof `data-id` link (NOT geometry). Lollipop (interface) markers map to `association` (documented approximation). Anything unrecoverable → `class-relation-unrecoverable` warning (surfaced as a parser-style diagnostic) + skipped. **STATE fidelity HIGH too:** endpoints by nearest-node-center matching (state edge ids don't encode ends), labels by `data-id` link — verified labels stay attached to the right transition even with unlabeled transitions in the mix (the earlier greedy-nearest approach mis-assigned; switched to direct `data-id` linking).
  - **Interactivity level (class/state = FULL, our node-graph strength):** both reuse the flowchart `vnmRuntime` unchanged → **fully draggable nodes with live edge re-routing** + pan/zoom/fit/minimap + layout persistence, in the exported HTML and via `mount`. The rich per-type UML markers + compartments live in the **static SVG/PNG**; the interactive DOM uses the generic vnmRuntime cards/arrow (dashed relations still show dashed) — the deliberate tradeoff that keeps the `dom-runtime-parity` guard green (no runtime fork). e2e confirms drag→re-route + pan/zoom + zero console errors in Chromium for both types.
  - **Notable for review/test/report:** (1) node-id recovery anchors the regex with `^.*` to the LAST `-classId-`/`-state-` token — the READ_ID (`vnm-state-read`) itself contains `-state-`, which a non-anchored regex mis-captured (caught + fixed). (2) A pre-existing CLI test used a class DSL to exercise the degraded-**fallback** `--strict` path; class is now native, so that test was re-pointed to an **ER** diagram (still fallback + degenerate headless). (3) State anti-parallel transitions between the same pair (e.g. Running⇄Paused) overlap visually — a known simple-dagre-routing aesthetic (same as flowchart), data is correct; a candidate for the hands-on aesthetic pass. (4) charts/manifest not regenerated this round (orchestrator drives the report/charts phase).
  - **round 3 (prior — native SEQUENCE, still green):**
  - **What shipped (files):** `src/model/sequence.ts` (SequenceModel + positioned SequenceLayout, sibling to the flowchart model). `src/native/sequence/`: `read.ts` (mermaid SVG → model, browser-safe: `loadMermaid` + `DOMParser`), `layout.ts` (our deterministic themed ordered layout), `svg.ts` (themed static SVG), `ascii.ts` (FR4 box-art), `interactive.ts` (payload + `mountSequence` + `renderSequenceHtml`), `index.ts` barrel. `src/render/dom/seq-runtime.ts` (self-contained pan/zoom/fit/minimap shell, `.toString()`-inlined into HTML export — no participant drag; that's the interactivity floor). Wiring: `router.ts` (sequence → tier `native`, renderer `"sequence"`, NO fallback diagnostic), `cli/run.ts` (`doSequenceRender`: read→layout→svg/md/html/png), and top-level `renderSvg`/`renderAscii`/`renderMarkdown`/`renderHtml` delegate a positioned `SequenceLayout`. `escapeXml`/`escapeXmlAttr` added to `render/style.ts` (shared sink escaping). Public API + `index.ts` export the sequence surface. Fixture `fixtures/order-sequence.mmd`.
  - **Read fidelity (high):** mermaid's sequence SVG carries direct hooks — `line/path[data-et="message"]` with `data-from`/`data-id`/`data-to` (logical from/to, no endpoint-x matching needed), `messageLine0`=solid vs `messageLine1`=dashed, `marker-end` for the arrow, `g[data-et="participant"][data-id]` + actor-box `<text>` for id/label (recovers `participant U as User` → id `U`, label `User`), `line[data-et="life-line"]` x for ordering. Labels paired to messages by document order (text emitted immediately before its line). **Recovered: participants(id/label/order), messages(from/to/label/solid|dashed/arrow/self/order).** **NOT recovered this round (deliberate scope):** notes (`data-et="note"` ignored), activations, loop/alt/opt fragments, message numbering. Geometry is discarded — we re-layout.
  - **Interactivity level:** themed + **pan / zoom / fit / minimap** (the plan's floor for sequence) via a separate self-contained `seqRuntime` that wraps our themed static SVG in a pannable world. NO participant drag (kept simple; the flowchart `vnmRuntime` + its parity guard are untouched — `dom-runtime-parity` still green). e2e confirms fit/zoom/pan/minimap + zero console errors in Chromium.
  - **For the class/state rounds (reuse + watch-outs):** (1) reuse the read pattern — `htmlLabels:false` + `loadMermaid` + `DOMParser`, walk `querySelectorAll('*')` for document order, attribute selectors over CSS class selectors (robust across jsdom/browser). (2) class/state geometry is **degenerate headless** (spike) → **re-layout with our dagre** (unlike sequence, whose geometry is sane but which we still re-layout). (3) reuse `escapeXml`/`escapeXmlAttr` from `render/style.ts` and the shared arrow-marker/edge-label-plate idiom. (4) for their node-graph interactivity, reuse the flowchart `vnmRuntime` (full drag) rather than `seqRuntime`. (5) `seqRuntime` is a good template for any "static-SVG + pan/zoom shell" (e.g. the fallback tier's pan/zoom shell in a later task).
  - **REV-001 (major/SECURITY, commit a094657):** theme tokens sanitized at the source before mermaid `themeVariables` — shared `isSafeColor`/`sanitizeFontFamily`/`sanitizeFontSize` in `src/render/style.ts` (color allowlist now single-source, reused by the v1 parser); unsafe values dropped/replaced + `unsafe-theme-value` FR5 diagnostic. Regression test reproduces the CSS-rule breakout + `url()` fetch.
  - **REV-002 (minor, commit 9f507e9):** jsdom `VirtualConsole` swallows `jsdomError` → no raw canvas/getContext stack traces on stderr; structured `render-failed` + exit 1 unchanged.
  - **REV-003 (minor, commit 8a4597e):** jsdom stood up only when NO usable DOM exists (`needsHeadlessDom()`), gated `inNodeRuntime() && needsHeadlessDom()`; a real/host DOM (Electron/jsdom test env) is never clobbered; degraded-geometry diagnostic keys on `usedHeadlessDom()`.
  - **REV-004 (nit, commit 0545453):** `await loadMermaid()` moved inside `classify()`'s try/catch → a loader failure routes native (`nativeFlowchart(null)`), decoupling the native path from mermaid being loadable (matters once D7 lands).
  - **round-1 context (implement round 1, prior):** spike + foundation built (`src/mermaid/`, `src/diagnostics/`, `src/export/png.ts`, CLI tier routing). Native renderers (sequence/class/state) NOT built yet — next round.
  - **Spike → `spike-01.md`.** Verdicts: **sequence = native-feasible (do first)**; **class + state = native-feasible via structure-read + OUR dagre re-layout** (mermaid's headless geometry is degenerate — don't reuse it); **user-journey = DROP TO FALLBACK** (bespoke timeline, low re-skin value — a plan refinement, was listed as a 4th native target). So **round-2 native scope = sequence + class + state (3, not 4)**.
  - **Foundation built:** `src/mermaid/` (lazy loader + `classify` detectType router + `renderFallbackSvg` browser/jsdom + token→themeVariables map + persistent jsdom-env), `src/diagnostics/` (FR5 channel), `src/export/png.ts` `renderPngFromSvg`, CLI wired (fallback tier, greppable stderr, `--quiet`, `--strict` escalation, silent-misparse fixed). mermaid+jsdom = deps, external+lazy — browser-safe core bundle unchanged (195KB).
  - **Load-bearing gotchas (see spike):** (1) must stand up jsdom **before** first `import("mermaid")` or its bundled DOMPurify freezes `.sanitize` missing process-wide; (2) `htmlLabels:false` required headless. Kanban (D2 fallback) degrades headless — render in browser for exact bounds.
- **open-decision:** none (for orchestrator to weigh at review: (a) drop journey from native per spike; (b) jsdom is a full runtime dep (~4MB, Node-only) — could be `optionalDependency` like resvg if browser-only install weight matters).
