# Adjustments — feature `dense-edge-routing`

Log of changes / clarifications requested during planning (and any UAT re-plans).

_No adjustments yet._

## 2026-07-15 — plan accepted

Scope **#1 (convergence tangle) + #2 (MCP skewer)**; **defer #3** (subgraph-aware routing → own feature). Convergence fix as a **new** `separateConvergentJogs` pass (v0.6.2 anti-parallel pass untouched). Sequence lifeline masking left as-is (separate; user chose not to fix now). No change to the technical approach.
