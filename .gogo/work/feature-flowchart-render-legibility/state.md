# State - feature `<slug>`

<!-- Files in this folder (.gogo/work/feature-<slug>/):
  - plan.md        - the accepted plan (the contract) + the feature's functional requirements
  - adjustments.md - log of changes / clarifications you asked for during planning
  - state.md       - THIS file: current phase / status / iterations; lets work resume
  - decisions.md   - forks that needed your call + gogo's recommendation + your answer
  - uat.md         - the UAT gate log: one round per user check after ⑤ (verbatim input + analyst analysis + plan delta + verdict); only appears once ⑤ reaches awaiting-uat
  - review/issues.json - living, typed review findings (the contract; see templates/contracts/)
  - review-NN.md   - each code-review round's rendered snapshot of issues.json
  - test/issues.json   - living, typed test findings (same contract)
  - test-NN.md     - each test round's rendered snapshot
  - events.jsonl   - append-only progress telemetry (one schema'd JSON line per phase transition; read by the gogo CLI; a missing file is never an error)
  - report/        - the as-built bundle (written at phase ⑤): report.md + the UML set (.mmd) + report/before/ (the plan-time "before" set, copied in for before/after compare) + diagrams.html + result.json
  - charts/        - mermaid diagrams (.mmd) + charts/before/ (the plan-time as-is baseline) + manifest.json + an offline diagrams.html viewer
  (and at the .gogo/ level: .gogo/changelog/<YYYY-MM-DD>-<slug>/ - the shipped report bundle archived by /gogo:done)

  UAT gate (from 0.11.0): phase ⑤ ends at status `awaiting-uat`, not `done`. The user verifies
  the work; running `/gogo:done` IS the acceptance. Or UAT feedback re-plans the SAME item: the
  orchestrator sets status `waiting-for-user` for the WHOLE re-plan stretch (so a mid-loop feature
  can't ship - /gogo:done needs awaiting-uat - or rerun - /gogo:go needs plan-accepted), records the
  round in uat.md, and only re-acceptance flips it to `plan-accepted`, after which /gogo:go reruns
  ②→⑤ back to awaiting-uat - `uat=N` tracks the loops.
-->

- **feature:** flowchart render legibility - stop edges covering subgraph titles & edge-labels, tighten label plates, and spread fan-in arrowheads (static SVG + inlined DOM runtime, parity-guarded)
- **phase:** done            <!-- plan | implement | review | test | knowledge | done -->
- **status:** shipped   <!-- awaiting-plan-acceptance | plan-accepted | implementing | reviewing | testing | waiting-for-user | awaiting-uat | done | shipped | aborted -->
- **created:** 2026-07-12
- **accepted:** 2026-07-13 (user) - UAT round 1 re-accepted: +FR9 edge-lane separation
- **branch:** fix/flowchart-render-legibility (cut from master at /gogo:go)
- **iterations:** plan=1 · implement=8 · review=6 · test=6 · uat=1   <!-- add · uat=N once a UAT round loops back to planning -->
- **resume:** none - shipped to .gogo/changelog/2026-07-13-flowchart-render-legibility/           <!-- <phase to re-enter> - <next action> | none -->
- **open-decision:** none    <!-- D13 resolved: accept & ship -->
- **report ⑤ (refreshed for FR9 + gap pivot + D12):** re-ran ⑤ after D12 green - refreshed report/report.md to the full as-built (FR1-FR9 + arc→gap pivot + D12 heading-order ports), refreshed report/flow.mmd + result.json, updated plan.md status, and extended the gogo-owned knowledge (code-review-standards: D12 coordinate-space parity rule + corpus-both-ways no-op proof + per-call-site guard/REV-009; test-strategy: FR9 lane / heading-order-port / state-reroute-parity journeys + assert-crossings->=1). TEST-004 → wontfix (documented follow-up, D13). state → phase=done, status=awaiting-uat. Nothing committed.
- **test r6 (UAT D12: API-fan de-knot):** D12 GREEN - all suites + every applicable hands-on check ran + passed, nothing blocked. tsc clean + build success + **388 unit (29 files)** incl. all 3 D12 guards + determinism (repro SVG ×2). Hands-on: static PNG traced - API's 3 outgoing edges monotonic (feed left 653.5 / alt-path straight 683.5 / hub right 713.5), 0 crossings; interactive HTML (served over local HTTP - file:// MCP-blocked, worked around) byte-matches static on load, 0 app console errors; LIVE drag of API Gateway re-routes cleanly (feed reassigns border side, alt-path/hub stay heading-ordered), 0 crossings/errors; examples clean incl. state fail/retry labels off their lines. e2e: found + fixed 2 PRE-EXISTING stale assertions unrelated to D12 (TEST-005 old `Q` bridge glyph → gap pattern; TEST-006 crossing count 1→2, both hand-verified real; proven pre-existing by toggling D12 off → byte-identical) → **79/79 e2e green**. TEST-004 (major, open) carried forward untouched per D12-only scope - the FR9 extreme-drag lane-convergence limitation (deferred comb-stagger); flagged for the D13 user decision, NOT silently resolved.
- **review r6 (UAT D12: API-fan de-knot):** APPROVE - 0 blockers/majors. Reviewed ONLY the D12 heading-order port change (rounds 1-5 already approved). Adversarially verified: byte-parity of the geometry↔runtime twin incl. the coordinate-space (dagre vs offset-removed) order-preservation + integer-exact `along` (no ULP tie flip); the committed reproDsl parity test genuinely reorders 4 source + 2 target ports and byte-compares (guards `tgtHead`, not just `srcHead`); backward-compat proven by running computePorts with-vs-without bends across the whole corpus (flowchart 42/171 edges carry bends → 0 port diffs; class 0 waypoints → D12 no-op, the ±10→±15 is pre-existing FR4 drift); determinism; state fan-in (`order-state`) static-vs-runtime match. One nit REV-009 (agent-fixable): the native/state re-route call site had only transitive parity coverage. **REV-009 FIXED (round 6, test-only):** added a dedicated `order-state` guard to dom-runtime-parity - baked `renderStateSvg` edges vs live `mountState`, offset-invariant relative-geometry compare; bite-verified (reverting native/state to center-order fails it). typecheck + **388 unit green** (+1). No product code touched for the fix.
- **implement r8 (UAT D12: de-knot the API Gateway outgoing fan):** ordered a shared border's ports by each edge's ACTUAL HEADING (first interior dagre bend at the source, last at the target) instead of the far node's centre - so an edge dagre steered sideways takes the port on that side instead of crossing a straight sibling. New optional `bends` param on `computePerimeterPorts` (fallback = far centre → straight edges byte-identical); wired from `layout()` (both call sites) + `native/state/layout.ts` (pseudo-reroute) + mirrored byte-for-byte in the runtime twin (`computePorts` reads `e.waypoints`; wp[0]/wp[last]); test helper `expectedPaths` updated to pass world-space bends. API→HUB now takes the rightmost port and peels away, alt-path→K2 drops straight down the middle port, feed→V1 takes the left - the knot is gone. typecheck + build + **387 unit green** (+2 guards: geometry `bends`-order unit test, layout API-fan no-inversion integration test); deterministic (repro SVG ×2); flowchart byte-parity (FR9 fixture, unchanged pass) + STATE byte-parity (live `mountState` vs static `renderStateSvg` — identical relative geometry) both confirmed; flowchart/class/state examples regenerated + eyeballed (state fail/retry labels also came off their lines). Class delta is pre-existing FR4 spread drift (my change is a no-op there — those edges have no detour waypoints). NOT committed. Deferred: left/right side attachment (follow-up).
- **implement r7 (UAT-round user-directed pivot: crossing GAPS + label-vs-edge):** (1) FR7 pivoted from arc **hops** to crossing **GAPS** - `applyEdgeBridges`/`gappedPath` (was `bridgedPath`) now break the more-VERTICAL (under) line with an ~8px pen-up gap (`L .. M ..`, GAP_RADIUS=4) at each crossing while the more-horizontal over-line stays continuous; same `bridges`/`--no-bridges` gate, corner+overlap guards kept; mirrored byte-for-byte in the runtime twin (radius/guards 5→4, 10→8; Q→M). (2) New `resolveLabelEdgeCollisions` (label-vs-edge) threaded into `finishEdges` between FR6 and label-vs-node - slides a label along its own edge off any PERPENDICULAR foreign crossing line (parallel grazes left alone); + LABEL_NODE_PAD 6→10 (a touch more node clearance); runtime twin mirrored. "gRPC stream" now clears both foreign verticals AND Ingress (0 foreign overlaps, ~10px node gap), moved right of the node. FR7/render-svg/parity tests updated (Q→gap); new layout assertion (gRPC 0 foreign segs); state snapshots + examples regenerated. typecheck + build + 385 unit (incl. dom-runtime-parity clean+sketch) green; repro PNG confirmed by eye (clean gaps, label clear, mid-channel + hub still separated); deterministic (repro SVG byte-identical ×2). NOT committed. Residual: "batch load" still grazes 1 PARALLEL neighbour lane (pre-existing, not an along-axis-fixable crossing).
- **test r4 (TEST-004 focused re-test):** ISSUES FOUND / done-bar NOT met - TEST-004 REOPENED (not verified). Build+typecheck+384 unit (29 files)+79 e2e all green, no regressions. But re-driving the EXACT round-3 repro (drag Ingress to world (138,320)) still collapses the mid-channel to 20/20px, confirmed via round-3's own unmodified debug-lanes.mts AND a live browser drag (real page.mouse events, 0 console errors). Root-caused (not guessed): the fix's own per-edge dedup rule creates a NEW order-dependent under-bundling gap - IN→HUB's true mid-channel segment gets excluded from the real 3-run bundle because its OTHER (unrelated) segment already claimed that edge's one dedup slot in sort order, so the bundle falls below LANE_MIN_BUNDLE=3 and never spreads. The shipped regression test's own 4 deltas are all mild nudges that don't exercise this failure. No other regression: determinism holds, static repro unchanged from round 3, fixtures no-churn, box 1/box 3 unaffected. Status back to `open` per routing rule; loops back to ② implement.
- **implement r6 (TEST-004 fix, round-4 found INCOMPLETE):** separateLanes now dedupes per-edge within a bundle (kills the zig-zag drift — bundle spreads 391/417/443, no 499) + LANE_PASSES 1→4 (converges post-drag, stable fixpoint); byte-identical in both twins → dom-runtime-parity GREEN (self-reviewed: parity-guard verifies the twin match). Added drag-reposition regression test + FR9 geometry/layout invariant tests. 384 unit green, deterministic, examples regen. Static + 4 drag repositions all hold mid-channel at 26px. **Round-4 re-test found this does NOT fix the original reported repro** (see test r4 above) - the 4 tested deltas were all mild and didn't exercise the actual failure geometry.
- **test r3 (FR9):** ISSUES FOUND / done-bar NOT met - build+typecheck+383 unit (29 files, incl. dom-runtime-parity FR9 lane fixtures)+79 e2e all green, no regressions. Hands-on (nothing blocked): box 2 (mid-channel, primary FR9 target) genuinely separated (26px lanes, was 20px), box 3 (hub fan-in) genuinely spread (28.4px gaps across 92% of hub width), both UAT-round-1 fixes (label-vs-node, hub spread) confirmed by direct measurement. Box 1 (Ingress-out fan) only marginally improved (27px vs pre-fix 26px, node-width-clamped, no comb-stagger shipped this round per the D10 reversal) - honest disclosed residual limitation, not a new defect, flagged for user judgment at UAT. Determinism + corpus no-churn + examples-current all confirmed. NEW major finding TEST-004 (agent-fixable): dragging a node - the plan's own Tests-table requirement - can leave `separateLanes` under-converged (`LANE_PASSES=1`), collapsing the mid-channel back to the exact pre-fix 20px merged defect; reproduced independently via live browser drag + a direct src/ library call (not guessed). Loops back to ② implement.
- **review r4 (FR9):** APPROVE - 0 blockers/majors; byte-parity + separateLanes safety + border-fill cap verified adversarially. REV-006 (stale D10 log) + REV-007 (FR9 invariant test coverage) fixed. 383 unit green.
- **implement r5 (FR9 twin + UAT-1 fixes):** (1) mirrored `separateLanes` + `shiftLabelOnSeg` byte-for-byte in the runtime twin (renderEdges + buildSvg, run FIRST before FR6/FR7; also fixed a latent labelPoly/labelBezier rounding drift so `labelPos` matches geometry before the lane pass reads it) → dom-runtime-parity GREEN incl. new clean+sketch lane fixtures; (2) label-vs-node de-collision (`resolveLabelNodeCollisions`, threaded into `finishEdges` via nodeBoxes; flow+state+class; runtime twin) — "gRPC stream" now clears Ingress (pushed down 12px); (3) hub fan-in: PORT_STEP 26→30 + border-filling cap `(borderLen−2·PORT_MARGIN)/(k−1)` replacing PORT_SPREAD_FRAC → 6-edge/154px hub fills to ±71 (anchorBound, no clamp), Ingress unclamped, FR4 fan tests hold. typecheck + build + 378 unit green (regen render/class/state snapshots + examples); repro PNG confirmed by eye (mid-lanes 26px apart, label clear, hub spread); deterministic.
- **D10 reversal (2026-07-13):** picked B (space-at-source/edgesep) but on seeing it the user found the global spread WORSE than the compact offset (5-fr9) - reverted to the OFFSET (separateLanes). User then "almost ok" on the offset, 2 issues left (label-over-node, hub fan-in). Offset is the settled FR9 approach.
- **open-decision:** none    <!-- <decisions.md anchor> | none -->
- **report ⑤:** as-built report written (report/report.md + flow+sequence diagrams + before/ compare + diagrams.html); knowledge updated. FR1-FR8 shipped + green.
- **UAT round 1 (2026-07-12 → re-accepted 2026-07-13):** user rejected at UAT - parallel edge lines run merged in a shared channel (Ingress outputs, batch-load/feed verticals, hub fan-in); "keep those lines separate" (the DEFERRED lane-routing case). Re-planned FR9 (separateLanes); re-accepted "full fix all 3 boxes" (D6-D9=A). Reruns ②→⑤.
- **open-decision:** none    <!-- <decisions.md anchor> | none -->
- **test r2:** GREEN/done-bar met - TEST-001 re-verified fixed (status: verified) by direct label-plate geometry measurement (zero AABB overlap, ~7.9px gap); build+typecheck+374 unit (29 files, incl. dom-runtime-parity)+79 e2e (73 pre-existing+6 new FR6/FR7 tests) all green; hands-on browser (gogo-playwright MCP) + PNG (6 theme/style combos) + determinism + D4 toggle + D5 sequence-exclusion all confirmed by eye/measurement, nothing blocked; found+fixed one new minor in-round (TEST-003, stale e2e assertion invalidated by FR7's own intended bridge-hop behaviour, test-file-only, no product code touched). No open/new issues.
- **review r3:** APPROVE/clean - REV-002/003/005 all verified resolved, REV-001/004 wontfix confirmed; no new findings; 374 unit + parity green.
- **implement r2:** FR6 (resolveLabelCollisions, fixes TEST-001) + FR7 (segmentsCross/applyEdgeBridges arc hops, flow+state+class) + D4 bridges config toggle (SvgRenderOptions.bridges / CLI --no-bridges / payload); all parity-mirrored.
- **review r2:** CHANGES - REV-002 (major): FR6 de-collided class/state labels with the tight size but native class/state SVG drew the OLD looser plate → could still overlap; REV-003 (minor): renderSvgAsync dropped bridges for class/state; REV-004/005 nits.
- **implement r3 (fixes):** REV-002 fixed (unified native class/state edgeLabel with labelPlateSize + drawn-plate guards, regen class/state snapshots+examples); REV-003 fixed (route.ts threads bridges); REV-005 fixed (bridgedPath skips overlapping hops, both twins + test); REV-004 wontfix (pre-existing viewBox-vs-label-plate, not observed, would churn all snapshots). typecheck clean; 374 unit green incl. parity.
- **review:** round 1 CLEAN (approve); REV-001 nit dispositioned wontfix/keep (shared-geometry ripple, verified non-breaking + good by eye)
- **test:** round 1 - build/typecheck/346 unit/73 e2e green (fixed a stale e2e selector, TEST-002); hands-on confirmed FR1/FR2/FR4 but found TEST-001 (open, major): "batch load"/"feed" edge-label plates overlap each other. D1=A: user chose to fix + EXPANDED scope (reserved-space margins + edge-crossing bridges/line-jumps for flow+activity; sequence excluded).
- **re-plan (2026-07-12):** additive plan written - FR1–FR5 kept as Done (baseline); +FR6 (resolveLabelCollisions, fixes TEST-001) +FR7 (applyEdgeBridges/segmentsCross, flowchart+state+class; sequence excluded) +FR8 (parity/determinism/churn). "activity" maps to flowchart/state (no activity type exists). OPEN forks D2–D5 for re-acceptance.
